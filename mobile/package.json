{
  "name": "hubfamily-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "HUB FAMILY Capacitor 앱 셸",
  "scripts": {
    "build:web": "npm --prefix ../webapp run build",
    "sync": "npm run build:web && cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "run:android": "npm run sync && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
